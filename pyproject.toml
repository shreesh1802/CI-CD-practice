[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "cicd-demo"
version = "0.1.0"
description = "Small demo project for learning GitHub Actions CI/CD"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["app"]